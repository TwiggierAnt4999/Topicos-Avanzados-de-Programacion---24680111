Para ejecutar, se debe crear un entorno virtual con Flet, una vez creado en una carpeta, el archivo main debe estar en la subcarpeta src

. La versión alterna del programa es exáctamente igual, solo que esta está en modo oscuro (para evitar fatiga visual)

